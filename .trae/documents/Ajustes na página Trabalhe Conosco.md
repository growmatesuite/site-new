## Objetivo
Atualizar o email de contato para `ops@growmate.io` e incluir o topo (Header) e rodapé (Footer) na página Trabalhe Conosco.

## Mudanças propostas
- Substituir o link e texto `rh@growmate.com` por `ops@growmate.io`.
- Importar `Header` e `Footer` e renderizar ambos na página.
- Manter a estrutura atual do formulário e estilos.

## Arquivo afetado
- `src/pages/WorkWithUs.tsx`

## Resultado esperado
- Página com header e footer consistentes com o restante do site.
- Email de contato atualizado para `ops@growmate.io`.

Posso aplicar essas mudanças agora?