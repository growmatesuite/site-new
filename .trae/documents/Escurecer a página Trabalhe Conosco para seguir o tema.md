## Objetivo
Aplicar tema escuro na página Trabalhe Conosco para ficar consistente com as demais páginas.

## Mudanças propostas
- Trocar o wrapper para `bg-black text-white`.
- Alterar o card do formulário de `bg-white` para `bg-black-secondary` com `border-gray-800`.
- Ajustar cores de textos: títulos em `text-white`, descrições em `text-gray-300`/`400`.
- Inputs/select/textarea com `bg-black-tertiary`, `text-white`, `placeholder-gray-600` e `border-gray-800`.
- Ajustar o texto do rodapé da página para `text-gray-400`; manter o link em verde.

## Arquivo afetado
- `src/pages/WorkWithUs.tsx`

## Resultado esperado
- Página com fundo escuro e componentes harmonizados com o tema (cores black.* e gray.* definidos em Tailwind).

Posso aplicar agora?