## Objetivo
- Aumentar a logo da Propbnk para ficar proporcional às demais na seção “Empresas que confiam na Growmate”.

## Implementação
- Em `src/components/sections/SocialProofSection.tsx`, aumentar a escala aplicada somente à Propbnk (de `scale-125` para `scale-150`).
- Manter Unimed com leve `scale-110` e preservar tom cinza (`brightness-0 invert opacity-60`).

## Verificação
- Recarregar a Home e checar se a Propbnk está visualmente do mesmo tamanho das outras.

Posso aplicar agora?